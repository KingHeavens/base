package algorithm;

/**
 * 0^N = N
 * N^N = 0
 * A^B = B^A
 * A^B^C = A^(B^C)
 *
 * XOR
 *
 * @author J.Heavens
 * @version 1.0
 * @since create at 2019/12/4
 **/
public class XOR {
    public int getOdd(int[] arr) {
        return -1;
    }

    public int[] getTwoOdd(int[] arr) {
        int[] result = new int[2];
        return result;
    }
}
